
Token - JWT dfgh.fghj.cfvgbn


AccessToken

    1) Подтверждает авторизацию пользователя
    2) Для доступа к защищенным ресурсам тоесть доступам к примеру роутам
    3) Годен какое-то время (обычно живет 1ч)

RefreshToken (как будто что то будет обновлять, освежать ТОЖЕ ИМЕЕТ СРОК ГОДНОСТИ - 30d)

//  в базе, храним только RefreshToken

1) Если срок годности AccessToken истек,
то вы обновляете ваш AccessToken при помощи RefreshToken

2) Если срок годности AccessToken истек И срок годности Refresh Token истек
то мы пользователя "гоним в шею из системы" (делаем logout)

















Для чего нужен Auth и где его использовать ?


1) Auth работает с токеном (JWT)
1.2) А зачем нужен токен?
1.3) Токен нужен чтобы проверять есть ли такой пользователь в системе и
не сгорел ли его доступ так как токены сгорают от 7 или от 30 дней

А что если токен сгорел?
Мы  просто просим пользователя залогиниться заново и тем самым делаем его обновление

По токену мы получаем именно ваш аккаунт в системе.
токен - доступ к вам

Auth в нашем API мы используем:
1) Когда есть какие-то закрытые страницы/роуты    /materials/:id, auth, (req, )
                                // 1 нахожу по токену юзера
                                // 2 Я проверяю какие у него права на этот роут



















JWT - JSON web token  - состоит из 3 частей, разделенными
точками

1) Header (Заголовок) - инф о типе токена и использ
в алгоритме шифрования

2) Payload (полезная нагрузка) - (!_id!, role, username)

3) Signature (подпись) - для проверки целостности и зашиты
от изменений

JWT -> 'Header.Payload.Signature'

Вход в систему -> получили JWT 